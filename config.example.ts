// This file is no longer used.
// The Gemini API key should be set as an environment variable `API_KEY`
// in your deployment environment.
// For example, when running locally, you might use a package like `dotenv`.
// When deploying, you would configure the environment variable in your hosting provider's settings.
